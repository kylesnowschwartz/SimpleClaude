{
  "name": "simpleclaude",
  "version": "0.2.1",
  "description": "A practical minimalist AI assistant framework",
  "scripts": {
    "token-check": "npx token-limit --config shared-simpleclaude-token-limit.config.ts"
  },
  "devDependencies": {
    "jiti": "^2.4.2",
    "token-limit": "^1.0.0"
  }
}
